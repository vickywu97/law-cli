# law-cli v1.0 · 法条速查器（数据合规 MVP）

## 这是什么
law-cli 是中文地方性法规 / 法律的**数据层最小闭环**：先解决最敏感的 IP 环节——法条原文的合法获取、固定来源、可复现证明，再谈查询与效力标注。所有法条原文取自官方公开渠道（依《著作权法》第五条属公共领域），每条固定完整证据链（来源 URL / 公布·施行日期 / 检索日期 / SHA-256），可离线复现。

## 核心数据（已发布）
- **219 条**法条，覆盖：
  - 《上海市生活垃圾管理条例》130 条（2019 通过 / 2026-08-15 修正，双 lineage 独立版本）
  - 《北京市生活垃圾管理条例》79 条（2020 修正）
  - 《著作权法》10 条（2010 + 2020 两版总则第 1–5 条）
- **全量 AI 审核终核（`ai_verified`）**：由 `ai_audit_all.py` 按「法 + 版本」选对应官方种子逐字对账，标记 AI 审核为最终状态，**无需律师签署复核**。
- **防篡改**：`verify` 逐条比对 SHA-256，**219/219 完好，0 篡改**。
- **复核闸门**：`verify --gate` 仅 `pending` 拦截，现全部 `ai_verified` → exit 0（发布前必跑，已接入 CI）。

## 功能
`fetch` · `show`（支持 `--version` 过滤）· `verify`（`--reconcile` 官方逐字对账 / `--gate` 复核闸门）· `versions`（版本轴 + difflib 差异对比）· `validity`（效力红黄绿）· `check`（最小引用校验）· `check-batch`（批量校验）· `relate`（对接本地 `verified-chinese-law-kb`）。

## 合规红线
- 官方源唯一化：全国性法律只从国家法律法规数据库 `flk.npc.gov.cn`；地方性法规锁定省级官方站（`*.gov.cn` 白名单），命中商业库（北大法宝 / 威科先行等）直接拒绝，需 `--allow-non-official` 才放行。
- 只提取条文原文，剥离编排 / 注释 / 效力说明等第三方增值内容。
- 完整证据链：来源 URL + 公布日 + 施行日 + 检索日 + SHA-256。
- 所有输出附法律免责横幅。

## 质量保障
- 单测 **15/15 通过**（纯标准库，零第三方依赖）。
- CI：每次 push / PR 自动跑单测 + `verify --gate` 终核闸门。
- MIT 许可。

## 快速开始（零依赖，仅标准库）
```bash
python3 law_cli.py show "著作权法.5"     # 查看单条 + 来源 + 哈希 + 免责
python3 law_cli.py verify --gate         # 终核闸门（全 ai_verified → exit 0）
python3 law_cli.py check "著作权法第5条"  # 引用校验
```

> ⚠️ **法律免责**：本工具仅整理公开法条原文，效力状态提示以官方发布为准，**不构成法律意见**，使用者须自行核实；具体事项请咨询具备相应资质的执业律师。
