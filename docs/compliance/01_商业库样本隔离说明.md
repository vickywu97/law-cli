# 商业库样本隔离说明（技术性证据记录）

> **性质声明**：本文件为 AI 辅助整理的**技术性证据记录与过程说明**，不构成法律意见，亦不构成合规结论。最终责任由本工具的责任人（发布者）承担。本说明仅记录"是否接触过商业数据库内容、如何处理、当前残留状态"等**可核验事实**，供责任人审阅与存档。

---

## 0. 背景

依据《真实态合规备忘录（初稿）》第 4 节记载：历史上曾接触过一份由**威科先行（wkinfo）**导出的 `.doc` 文件，该文件内含其专属超链接（如 `law.wkinfo.com.cn` 等）及分类目录、效力标注等**增值内容**。该文件从未作为任何法条条文的来源；其存在仅作为"风险样本"被记录。

本说明对该记载做**当前状态核验**与**隔离政策留痕**。

---

## 1. 当前技术核验结果（2026-08-16，AI 执行，read-only）

| 核验项 | 方法 | 结果 |
|---|---|---|
| 数据库商业库域名残留 | 扫描 `data/law_db.json` 全部 154 条 `source.url`，匹配 wkinfo / pkulaw / wefind / westlaw / lexisnexis / law.wkinfo / chinalawinfo / win4u / intelleva 等付费库域名关键词 | **0 命中** |
| 来源域名构成 | 同上扫描统计 | 仅 `flk.npc.gov.cn`(5) / `www.nppa.gov.cn`(5) / `www.beijing.gov.cn`(79) / `www.jiading.gov.cn`(65)，**全部为官方政府/立法域** |
| 工作区样本文件残留 | 在 `projects/law-cli/`（排除 `.git`）检索 `*.doc` / `*.docx` | **0 命中**（无任何 `.doc`/`.docx` 文件） |
| 工作区字符串残留 | 全项目检索 `wkinfo` / `pkulaw` / `威科` | 仅出现在 README、备忘录、调研文档等**讨论隔离策略的文本**中；**任何数据文件均不含**此类字符串 |
| 数据完整性 | 重算每条 `text` 的 SHA-256 与存储值比对 | **154/154 完全一致**，零篡改 |

> **误报纠正记录**：初次复核脚本曾误将 `flk.` 列入付费库黑名单，导致 `flk.npc.gov.cn`（国家法律法规数据库，官方域）被误计为 5 条"命中"。已剔除 `flk.`，修正后真实付费库命中为 0。特此说明，避免后续审阅者被误导。

---

## 2. 隔离政策与处理结论

1. **未采用原则**：威科先行 `.doc` 样本所含的商业库增值内容（目录、效力标注、注释、案例链接等）**从未写入** `data/law_db.json` 的任何条文记录；数据库条文仅含"官方条文纯文本"。
2. **当前残留状态**：经第 1 节核验，**当前工作区（含数据、代码、文档）不存在商业库样本文件或商业库域名痕迹**。
3. **版本控制留痕**：本仓库（含 `.git` 历史）未纳入任何 `.doc`/`.docx` 商业库样本；如有历史备份位于仓库之外，责任人应另行确认其物理隔离位置（不在 `seed/`、`data/` 内即可）。

---

## 3. 建议责任人动作（非阻塞）

- [ ] 确认本地及云端备份（如 WorkBuddy 工作区快照、其他副本）中**亦无** wkinfo `.doc` 样本残留。
- [ ] 将本说明随合规档案一并存档，作为"曾接触、未采用"的自证材料。
- [ ] 若后续任何流程确需参考商业库，须先经责任人评估并显式记录，禁止静默引入。

## 4. 物理隔离确认（责任人填写）

本人已确认以下位置**不存在**威科先行（wkinfo）`.doc` 样本或任何商业库导出文件：

- [ ] 本机工作目录（含 `projects/law-cli/`）
- [ ] WorkBuddy 工作区及快照
- [ ] 备份磁盘 / 云盘（iCloud 等）
- [ ] 其他临时文件夹

> 当前工作区技术自查（2026-08-16，AI 执行 read-only）：全项目检索 `*.doc`/`*.docx` = 0 命中；`data/law_db.json` 154 条来源域名全为官方域，付费库域名 0 命中。但**自查仅覆盖工作区，不覆盖工作区之外的备份/云盘**，故须责任人按上表逐项确认。

隔离 / 删除动作记录：

- 动作：删除 / 移至隔离目录
- 时间：____年____月____日
- 位置：________________

签字：________________

---

*本文件由 AI 根据当前工作区实际状态生成，仅供责任人（发布者）审阅与决策参考。法律合规的最终判断与责任归属不因本文件而转移。*
